import React from 'react';
import Banner from '../../components/Banner'

function NotFound(props) {
    return (
        <div>
            <Banner title="Oopss... Page not found !" />
        </div>
    );
}

export default NotFound;